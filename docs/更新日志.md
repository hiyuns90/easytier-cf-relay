# 更新日志

> 本文件记录每个发布版本的变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
> 版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。
> 变更背后的协议细节与设计依据见《[技术文档](./技术文档.md)》，
> 配置项与部署说明见《[部署手册](./部署手册.md)》。

## \[1.4.1\] - 2026-09-19

模型：GLM5.3-Flash；本次花费：15 RMB；总花费：113 RMB；
安全加固与运维小版本：修复一个管理端鉴权缺陷，补事前限流、KV 键前缀更名与站点图标。

### 修复（重大）

* **DO 内部端点的鉴权可达性缺陷**：管理端内部端点（`/internal/*`）可能被
  特制请求绕过 Worker 入口的 token 鉴权直达。现统一在 Durable Object 层
  拦截此类请求（一律 404），内部端点只接受 Worker 入口鉴权后的调用。

### 新增

* **事前限流：单 IP 并发连接上限**（`MAX_CONNS_PER_IP`，默认 6，`0` 关闭）：
  同一 IP 的开放连接数达到上限后新连接请求返回 429 + `Retry-After`，拦截次数
  计入总览页「IP 限流」（`counters.ipLimited`）。黑名单是事后拦截，此检查
  补上事前缺口；客户端 IP 现随连接元数据持久化（顺带修复：DO 休眠重启后
  连接列表丢 IP 的小问题）。
* **未知路径挂起不响应**（`TARPIT_UNKNOWN`，默认开，`0` 恢复 404）：非 WebSocket
  升级请求访问未知路径时挂起——不返回 404、不暴露行为指纹；EasyTier 客户端
  只走升级、保留路径各有响应，均不受影响。
* **站点图标**：`/favicon.ico` 返回内嵌的 128×128 项目图标（用Easytier官方图标当占位图）（原为 404），
  浏览器缓存 1 天；管理页 `<head>` 同步声明。

### 变更

* **KV 键前缀更名**：`et-relay:` → `easytier-cf-relay:`。首次启动
  自动一次性迁移旧键数据（新前缀完全为空才回读旧前缀，新旧绝不混用）；
  旧键保留不删，确认运行正常后可在 Cloudflare 控制台手动清理。
* KV 镜像刷盘分批写（每批 ≤6 并发）：对齐免费计划「单次调用同时打开 6 个
  连接」的限制，避免多个脏键一次性并发写触发运行时排队。

## \[1.4.0\] - 2026-09-13

模型：GLM5.3；本次花费：13 RMB；总花费：98 RMB；
面向免费额度的下一代省额度优化（v2 迭代第一弹）：alarm 自适应排程、边缘黑名单
缓存、管理页刷新节流，以及 `/health` 路径自定义。协议层无变更，与 1.3.0 完全兼容。

### 修复（重大）

* **`_markDirty()` 闹钟抖动导致清扫饥饿**：上一版版注释说"若当前无近期 alarm 才安排
  短 alarm"，代码却无条件 `setAlarm(now+2000)`。客户端持续抖动重连时（握手/清理
  事件间隔 <2s），闹钟被不断推到"最新事件 +2s"——只要抖动持续，alarm 永远落在
  未来，幽灵节点清理、路由老化、空分组删除、脏状态落盘全部停摆（事件停止后
  2s 可恢复，非死锁）；批量上线时还产生冗余 storage 写。现先 `getAlarm()`，
  仅当无闹钟或已有闹钟比目标更晚时才设置，**绝不推迟更早的闹钟**。

### 变更

* **alarm 自适应排程**（`src/room.js`）：重挂间隔不再固定 15s，由 `_nextAlarmDelay()`
  计算——有连接时取 `min(SWEEP_INTERVAL_MS, 最近的 socket 事件时刻)`（未握手
  超时截止/探活触发/空闲超时截止，下限 1s）；完全空闲（无连接且无脏数据/审计
  待刷）退避到新配置 `SWEEP_IDLE_INTERVAL_MS`（默认 300000ms）。空房间 alarm
  消耗从 5,760 请求/天降至 **288**；有连接时也不再无意义地固定 15s 一醒。
* **边缘 IP 黑名单 isolate 缓存**（`src/index.js`）：WS 升级前的 `bl:socket` KV 读
  加 TTL 缓存（新配置 `IP_BLOCK_CACHE_MS`，默认 30000ms，`"0"` 关闭），重连风暴
  时 KV 读从每次连接 1 次降为每 TTL 1 次。**只延后拦截、绝不漏放**：缓存未命中
  （含 KV 读失败）照旧放行走 DO 层权威黑名单检查，仅新拉黑 IP 的边缘拦截至多
  延后「KV 最终一致窗口 + TTL」。
* **管理页自动刷新省额度**（`src/admin_ui.js`）：间隔 10s → 30s，`setInterval` 改
  递归 `setTimeout`，页面不可见（后台标签页/最小化）时暂停、可见时恢复。长开
  页面请求量约 8.6k → 2.9k/天，后台挂页归零。

### 新增

* **`/health` 路径自定义**（`src/index.js`）：新配置 `HEALTH_PATH`（默认 `/health`，
  行为不变）。配置后仅该路径返回 `{"ok":true}`，原 `/health` 返回 404（防指纹
  扫描，无需 token）；WebSocket 保留路径同步纳入。

### 配置

* 新增 `SWEEP_IDLE_INTERVAL_MS`（默认 `300000`，`0` 关闭退避）、
  `IP_BLOCK_CACHE_MS`（默认 `30000`，`0` 关闭缓存）、`HEALTH_PATH`（默认
  `/health`）；`SERVER_VERSION_STR` 默认值更新为 `easytier-cf-relay/1.4.0`。

## \[1.3.0\] - 2026-09-05

模型：GLM5.3；本次花费：9 RMB；总花费：85 RMB；
面向免费额度与可观测性的审计/黑名单优化，协议层无变更，与 1.2.0 完全兼容。

### 修复（重大）

* **管理页所有列表的「操作」列按钮无实际效果**：`esc()` 转义函数未处理双引号，
  `data-payload="{"id":5}"` 的 JSON 引号在 HTML 属性中被截断（按钮拿到的 payload
  变成残缺 JSON，解析失败退化为空对象），点击后服务端收不到目标参数。
  现将双引号一并转义为 `&quot;`（文本与属性双上下文安全），全部列表的
  单行操作（踢出 / 删除分组 / 删除路由 / 删除互联 / 断开 / 删除摘要 / 删除记录 /
  移除黑名单）真实生效——**无需勾选复选框即可执行**（勾选仅用于顶部批量按钮）。

### 改进 — 审计记录（防刷屏 + 省额度）

* **黑名单拒绝事件不再写入「节点在线」记录**：被拉黑客户端会不断重连，
  逐条记录会刷爆记录列表并徒增 DO storage/KV 写入；拒绝改由总览页新增的
  **「黑名单拦截」计数器**（`counters.blRejected`）观测，零写入成本。
* **管理端「查看」事件不再记录**：管理页 10s 自动刷新会持续产生查看记录
  （原先按 5 分钟折叠计数），既刷屏又浪费写入；登录（IP/时间，10 分钟去重）
  与操作明细仍为硬审计。

### 改进 — 黑名单拦截（双层，省 DO 额度）

* 新增 **Worker 入口边缘层 IP 拦截**：WebSocket 升级前直接读 KV 的
  `bl:socket` 一条键，命中立即 403（附 `Retry-After: 60`）——**不唤醒
  Durable Object**，被拉黑客户端的重连风暴只消耗 Worker 请求额度，
  不再消耗 DO 请求与时长计费。
* DO 层（升级 / 握手）保留为权威兜底：覆盖 KV 最终一致窗口（约 1 分钟）内
  新拉黑的 IP；KV 读失败时边缘层放行、DO 层拦截（边缘 fail-open、DO fail-closed）。
* 解除封锁的生效时间 = KV 传播时间（通常数秒，最长约 1 分钟）。
* 未绑定 `AUDIT_KV` 时边缘层自动跳过，行为与 1.2.0 一致。

### 新增 — 记录查询「全部」视图

* 记录类型下拉首项新增**「全部」**：七类记录（六类数据 + 管理端审计）在内存
  合并，按全局 id（= 时间序）倒序分页，附「类型」列——不引入任何新 KV 键。
* 「全部」模式支持按 id 跨类型删除（单个 / 批量）；admin 硬记录仅展示，
  该行的勾选与删除按钮均禁用（服务端同样拒绝，双保险）。

### 其它

* 403 拒绝响应附带 `Retry-After: 60`，建议客户端退避重试。
* 文档：技术文档 §9 重写拦截层次与成本说明；部署手册新增 Q11（重连风暴与免费额度）、
  更新成本核算与 KV 读额度说明；多房间分片下 KV 审计的共享键限制已注明。

### 验证

* 语法与构建静态检查通过；协议行为（对齐 easytier-core 2.6.4 官方客户端）、
  管理端全部页面的行内操作与批量操作、KV 审计/黑名单全链路（含边缘层拦截
  不唤醒 DO、「全部」合并查询与跨类型删除）均经回归验证通过。

## \[1.2.0\] - 2026-08-31

模型：GLM5.3；本次花费：15 RMB；总花费：76 RMB；
在 1.0.0 协议完备的基础上补齐「可观测 + 可管控」：新增 KV 审计记录与管理端黑名单，
管理控制台新增三个页面（记录查询 / 记录设置 / 黑名单）。协议层无变更，与 1.0.0 完全兼容。

### 新增 — KV 审计记录

* 六类网络事件记录进 Workers KV：网络分组 / 节点在线 / 路由信息 / 全局互联 / 连接列表 /
  摘要注册（**每类只占一条 KV 键**，定长环形缓冲；连同管理端审计与黑名单全生命周期共 11 条键）
* 事件明细：分组 create / delete（含原因）、节点 join / leave / replace / reject（含 IP）、
  路由 add / remove（direct / transit 与顶替记录）、互联 add / remove（含 TTL 过期）、
  连接 open / close / error（含 socketId、IP）、摘要 register / delete
* 管理端硬审计：管理员登录（IP / 时间）、查看（5 分钟折叠计数）、操作明细；
  `ADMIN_AUDIT` 为硬开关，管理页不可关闭，admin 类记录不可删除（防审计自擦除）
* 三级写入路径控成本：事件先入内存数组 → alarm 周期落 DO storage（防休眠丢失）→
  KV 镜像仅写脏键并按 `RECORD_FLUSH_MS`（默认 10 分钟）节流；管理操作完成后强制刷 KV
  （黑名单立即生效）。免费额度 1000 写/天下默认每键最多 144 写/天，典型小网络每天合计数十次
* 未配置 `AUDIT_KV` 绑定时优雅降级为仅 DO storage 存储，管理页功能不受影响

### 新增 — 管理端黑名单

* 四类黑名单各占一条 KV 键：节点 PeerId / 网络分组（网络名）/ 摘要注册 / 客户端 IP
* 管理操作自动拉黑：踢出节点 → peer；删除分组 → group（网络名）；删除摘要注册 → digest；
  断开连接 → socket（已知客户端 IP）
* 双拦截点：WebSocket 升级时按客户端 IP 403 拒绝；握手（摘要校验前）按 peer / group / digest
  关闭连接（4013 blacklisted），拒绝事件记入节点记录
* 黑名单页支持手工添加（含备注原因）、单个与批量移除（解除封锁）、整类清空；
  上限 `BLACKLIST_LIMIT`（默认 1000）为硬设置

### 新增 — Web 管理端（三个新页面）

* 记录查询：按类型筛选 + 分页浏览六类记录与管理端审计，支持单个与批量删除（admin 类不可删）
* 记录设置：六类记录各自的开 / 关与条数上限（软设置，运行时生效，收紧上限时立即裁剪旧记录）
* 黑名单：四类黑名单的查询 / 添加 / 移除 / 清空，含各类条数统计
* 侧边栏新增「记录查询 / 记录设置 / 黑名单」功能块与统计徽标，总览页汇总审计统计

### 新增 — 配置项（wrangler.toml）

* `AUDIT_KV` KV namespace 绑定（可选）
* `RECORD_FLUSH_MS`（KV 镜像节流间隔，默认 600000）
* `RECORD_DEFAULT_LIMIT`（各类记录默认上限，默认 100）
* `BLACKLIST_LIMIT`（各黑名单上限，默认 1000）
* `ADMIN_AUDIT` / `ADMIN_AUDIT_LIMIT`（管理端审计硬开关与上限，默认开 / 200）

### 修复

* 运行时长在 DO 休眠或重启后重新计时的问题：首次启动时间持久化到 DO storage，
  现按房间首次创建起累计（含休眠时间），管理页附说明提示
* 管理端命名与统计口径统一：连接（sockets）与管理端审计纳入总览统计，
  侧边栏徽标与总览汇总一致

### 文档

* 技术文档新增 §9「KV 审计与管理端黑名单」；部署手册新增 5.6 节（KV 审计配置）并更新成本章节；
  README 同步功能清单

## \[1.0.0\] - 2026-08-26

模型：GLM5.3；本次花费：61 RMB；总花费：61 RMB；
首个公开发布版本。协议行为以官方 easytier-core **v2.6.4** 源码为基准逐项对齐，
并使用官方客户端实测验证：握手、路由同步、双节点互见、数据中继、长稳运行全部通过。

### 新增 — 协议层（对标官方 easytier-core 2.6.4）

* ZCPacket / PeerManagerHeader（16 字节小端头）解析与构造

* WebSocket 隧道：接受任意路径的升级请求（与官方服务端行为一致）

* 握手：magic / version / features 镜像 / 网络摘要校验
  （SipHash-1-3，与官方同算法同密钥派生）

* Ping/Pong 心跳：payload 原样回显（支持客户端自增 seq），liveness-echo flags 镜像

* RPC 分发：RpcPacket / RpcRequest / RpcResponse，int64 transaction\_id 全精度

* OspfRouteRpc.SyncRouteInfo：请求处理 + 响应 + 主动推送（全量 / 增量）

* 路由增量同步：version / peer\_route\_id / session 语义（peer\_route\_id 变化即无条件替换）

* RoutePeerInfo 未知字段逐字节保留（wire 层重组，对标官方 raw\_peer\_infos 机制）

* PeerCenterRpc.ReportPeers / GetGlobalPeerMap：官方 replace 语义 + digest 缓存

* 包转发：forward\_counter 每跳 +1、>7 丢弃、转发时清除 LATENCY\_FIRST

* 同 peerId 重连顶替：旧连接关闭 + 路由更新广播

* 加密包（ENCRYPTED flag）透传不受影响

* 网络分组隔离：网络名 + 摘要 → 独立分组，互不可见

### 新增 — 平台层（Cloudflare Workers）

* Worker 入口：`/health` 零指纹；`/metrics` 与 Web 管理端为自定义安全路径 + token
  双门禁（fail-closed，不配置即 404，零攻击面）

* RelayRoom Durable Object：全 WebSocket Hibernation API，全程零 setInterval/setTimeout 常驻

* Alarm 驱动周期清扫（DO 可休眠，duration 成本趋零，免费额度内稳定运行）

* attachment 元数据：休眠唤醒后恢复 peerId / groupKey / lastSeen

* 路由状态持久化：脏标记 + 节流落盘，损坏数据静默丢弃

* 多房间分片（Header / Query 可配置）

### 新增 — Web 管理端

* 侧边栏列表式导航：总览 / 网络分组 / 节点在线 / 路由信息 / 全局互联 / 连接列表 / 摘要注册表

* 服务端分页 + 各列表独立滚动：节点数量大时打开管理页不卡不爆

* 各功能块自带统计（侧边栏徽标 + 总览页汇总）

* 全功能块批量操作（单个与批量均支持）：踢出节点 / 删除分组 / 删除路由条目 /
  删除互联条目 / 断开连接 / 删除摘要注册

* 路由信息与摘要注册表支持单独删除

* 官方 easytier-cli 命令对照表

* 数据 API 全部经 token 鉴权（页面壳公开、数据不公开）

### 新增 — 稳定性与自动清理

* 幽灵节点六重防线（详见技术文档 §6）：

  * 连接级：握手超时（15s）/ 空闲超时（75s）/ 服务端主动探活（40s）

  * 路由级：direct/transit 来源标记（transit 永不覆盖 direct）+ reporter 追踪 /
    路由老化（对齐官方 `clear_expired_peer` 两层语义：不可达 90s / TTL 3660s）/
    空分组自动删除（宽限 60s）

* 空分组自动删除：分组内在线节点归零并持续超过宽限期 → 整组删除（含路由条目与摘要注册）

* 摘要注册表自愈：孤儿摘要注册随分组删除一并解除，不留脏数据

### 新增 — 安全加固

* STRICT\_DIGEST：同一网络名出现不同摘要时拒绝（防网络名抢占）

* NETWORK\_SECRETS：服务端侧网络密钥校验（SipHash-1-3 摘要比对）

* MAX\_MESSAGE\_BYTES / MAX\_PEERS\_PER\_ROOM 资源限制

* 转发前校验 from\_peer\_id 与连接注册身份一致（伪造源丢弃并计数）

* 路由 transit 条目不覆盖 direct 条目（防路由投毒 / 防幽灵复活）

* PeerCenter 上报以连接注册身份为准（防身份冒用）

### 修复（开发过程中定位并解决）

* **幽灵节点（重大）**：客户端多 PeerId 竞争后，被放弃的 PeerId 作为 transit 条目
  被其他节点转述上报，导致路由表出现"无在线连接的幽灵条目"（节点实际只有一个连接）。
  通过 reporter 追踪（上报者断开即清除其上报的无主 transit 条目）+ direct 覆盖规则 +
  路由老化三层机制根除，同类残留此后自动清理。

* 管理端"官方 easytier-cli 命令对照"表格右侧溢出：容器改为允许横向滚动。

* `wrangler deploy` 多环境警告：deploy / dry-run 脚本显式指定 `--env=""`，
  明确使用顶层环境，避免误部署到错误环境。

### 已知限制

* 不支持 secure mode（Noise，v2.6.0+ 可选加密握手）——客户端使用默认模式即可

* 不执行 P2P 打洞（Workers 无 UDP 出口），仅提供 rendezvous；双端严格 NAT 时走中继

* 不做 KCP/QUIC 协议转换（相关包按 to\_peer\_id 原样转发，无功能影响）

* 单房间为 DO 单线程串行（软限制 \~1000 req/s）；更大规模使用房间分片

完整列表见技术文档 §11。
